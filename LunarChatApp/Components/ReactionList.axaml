<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialIcons="using:Material.Icons.Avalonia"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="LunarChatApp.ReactionList">
  <WrapPanel Grid.Row="1" Margin="0,10,0,0">
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
				<Button Padding="10,6,10,6" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Background="{DynamicResource BorderColor60}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<materialIcons:MaterialIcon Kind="House" Width="18" Height="18" Foreground="{DynamicResource PrimaryColor}" />
						<TextBlock FontSize="14" Text="1" HorizontalAlignment="Center"></TextBlock>
					</StackPanel>
				</Button>
			</WrapPanel>
</UserControl>
